# Website environment.
# In dev, leave VITE_API_URL unset — the vite.config.ts proxies /api to
# localhost:5000. In production, the built site sits behind Caddy which
# forwards /api itself, so VITE_API_URL=/api is correct (set via Dockerfile arg).

VITE_API_URL=/api
VITE_SOCKET_URL=
